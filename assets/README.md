# assets

Screenshots / demo recordings referenced by the top-level README.

- `demo.png`  — the pulse running (a tick: sense → decide → act → journal)
- `demo.gif`  — optional short screen recording of a few beats

Capture tips:
- run `looop` in a clean tmux pane; the tick output is colorized and self-explanatory
- a GIF (e.g. via `vhs`, `asciinema`+`agg`, or `terminalizer`) shows the loop best
